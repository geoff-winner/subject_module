<?php

function subject_form_alter(&$form, &$form_state, $form_id) {
    if($form_id == 'comment_node_page_form') {
        $form['subject'] = array('#default_value' => 0,
    );
        // print '<pre>';
        // print_r($form);
        // print '</pre>';
    }
}
